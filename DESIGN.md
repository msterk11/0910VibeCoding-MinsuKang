# DESIGN.md

## 이게 뭐야

강민수의 한 페이지짜리 자기소개 사이트. 방문자가 스크롤 한 번으로 이름·소속,
진행한 프로젝트, 관심사, 취미, 연락 링크를 볼 수 있다. 콘텐츠는 로컬 JSON 파일
하나에서 읽고, 화면은 90년대 CRT 터미널을 흉내 낸 모습이다.

## 화면에 있는 것들

위에서 아래로 하나의 세로 흐름. 전체가 옛날 터미널 창처럼 얇은 테두리로 감싸여 있다.

- **창 타이틀바** — 왼쪽에 `portfolio.sh`, 오른쪽에 `80×24` (모바일에선 숨김)
- **로그인 줄** — `Last login: Thu Sep 10 2026 on ttys001`
- **이름** — `$ whoami` 아래에 "강민수" (가장 큰 글자, 자간 넓게, 앰버색)
- **소속** — `$ cat ./affiliation` 아래에 "고려대학교 / 컴퓨터학과"
- **소개 글** — `$ cat ./about.txt` 아래에 한 문단
- **프로젝트 목록** — `$ ls -la ~/projects` 아래에 항목들.
  각 항목은 `drwxr-xr-x  미리me/` 같은 디렉터리 목록 형태 + 설명 줄 + `# role:` 주석 줄
- **관심사 목록** — `$ cat ./interests.txt` 아래에 번호 매긴 3줄
- **취미 목록** — `$ cat ./hobbies.txt` 아래에 번호 매긴 2줄
- **링크 목록** — `$ cat ./links.txt` 아래에 `[0] GitHub → 주소`, `[1] Email → 주소`.
  링크 글자에 밑줄, 마우스를 올리면 앰버 배경으로 반전됨
- **프롬프트 줄** — 맨 아래 `minsu@portfolio:~$` 뒤에 깜빡이는 네모 커서

별도의 버튼은 없다. 클릭 가능한 요소는 링크 목록의 GitHub / Email 두 개뿐이다.

섹션 제목은 전부 셸 명령어처럼 보이는 회색 글씨이고, 실제로 실행되지는 않는 장식이다.

## 색

| 이름 | 값 | 쓰는 곳 |
| --- | --- | --- |
| 화면 배경 | `#0b0f0b` (거의 검정) | 페이지 전체 배경 |
| 인광 그린 | `#33ff66` | 기본 글자색, 커서 |
| 흐린 그린 | `#1f9d4d` | 섹션 제목(명령어), 주석, 부가 설명, 테두리 |
| 앰버 | `#ffb000` | 이름, 프로젝트 이름, 링크 강조, 링크 호버 배경 |

- 모든 글자에 옅은 초록빛 발광(글로우) 효과
- 화면 위에 가로 스캔라인이 깔려 있음
- 라이트 모드 없음. 항상 이 어두운 화면 하나

## 글꼴

- 본문·제목 전부 **고정폭(모노스페이스)** 글꼴 하나만 사용
- 기기에 있는 것 중 순서대로: SF Mono → Menlo → Consolas → Liberation Mono →
  D2Coding → 시스템 기본 고정폭
- 한글도 같은 고정폭으로 표시 (D2Coding이 있으면 그걸로)
- 크기는 두 단계뿐: 이름(가장 큼), 나머지 본문(한 크기). 모바일에선 본문이 살짝 작아짐
